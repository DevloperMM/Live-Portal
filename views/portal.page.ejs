<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>HodlInfo</title>
  </head>
  <body>
    <div class="container">
      <h1>Top 10 Cryptos from WazirX</h1>
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Last Price</th>
            <th>Buy Price</th>
            <th>Sell Price</th>
            <th>Volume</th>
            <th>Base Unit</th>
          </tr>
        </thead>
        <tbody>
          <% result.forEach(function(data) { %>
          <tr>
            <td><%= data.name %></td>
            <td><%= data.last %></td>
            <td><%= data.buy %></td>
            <td><%= data.sell %></td>
            <td><%= data.volume %></td>
            <td><%= data.base_unit %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </body>
</html>
